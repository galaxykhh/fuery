name: fuery
description: "Server data caching for Flutter: cache API responses, refetch in the background, paginate, mutate with optimistic updates, and keep data across restarts."
version: 1.4.2
homepage: https://galaxykhh.github.io/fuery/
repository: https://github.com/galaxykhh/fuery/tree/main/packages/fuery
documentation: https://galaxykhh.github.io/fuery/
topics:
  - state-management
  - server-state
  - cache
  - caching
  - offline

resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  fuery_core: ^1.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
